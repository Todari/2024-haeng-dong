name: Deploy Server

on:
  push:
    branches: [main]
    paths:
      - "apps/server/**"
      - "packages/shared/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script_stop: true
          command_timeout: 10m
          script: |
            cd ~/2024-haeng-dong
            git pull origin main
            cd ~
            docker compose up -d --build haengdong-server
            sleep 15
            curl -s -o /dev/null -w "%{http_code}" http://localhost:8080 | grep -q "404\|200" || exit 1
            echo "Deploy successful!"
